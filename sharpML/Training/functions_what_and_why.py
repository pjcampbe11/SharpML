#!/usr/bin/env python

L = [2,3,4,5,3,8,4,2]

maximum = L[0]
for i in range(1,len(L)):
    if(L[i] > maximum):
        maximum = L[i]
print(maximum)

find_maximum(array)

find_maximum(L)
find_maximum(A)